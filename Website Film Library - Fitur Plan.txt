*MAIN PLAN
1. Pembuatan database dan mengisi 10 data list film
	i. Nama Database => filmlibrary
	ii. Nama nama kolom dalam 'filmlibrary
		a. id -> primary_key
		b. namaFilm -> varchar(100)
		c. tahunTerbit -> date
		d. deskripsiSingkat -> varchar(255)
		e. rating -> varchar(100)
	iii. Tambah 10 data list via CLI
2. Pembuatan index.php yang berisi: 
	i. Tabel tampilan list film dan Query film dari database
	ii. Pengkondisian pesan (error maupun message berhasil)
	iii. Paginasi list film dari database
	iv. Buat fitur pencarian data
	v. Buat fitur tambah data
	vi. Buat pemrosesan data file
	vii. Buat fitur edit data
	viii. Buat fitur hapus data
3. Tambah fitur registrasi
	i. Buat database users
	ii. Buat halaman registrasi
3. Tambah fitur login
	1. Buat tampilan login
	2. Buat logic login dan tetapkan SESSION di:
		a. login.php
		b. index.php
		c. logout.php
		d. tambah.php
		e. edit.php
		f. hapus.php
	3. Buat fitur Remember Me dan Auto Login menggunakan COOKIE
	4. Buat fitur logout




*NOTE:
1. Database
	1. 'id' int PRIMARY KEY AUTO_INCREMENT
	2. 'filmName' varchar(100)
	3. 'tahunTerbit' date
	4. 'descripsiSingkat' varchar(255)
	5. 'rating' varchar(100) 
2. Sintaks SQL
	a. Create Database
		-> CREATE DATABASE nama_database
		-> use nama_database
	b. Create Table
		-> CREATE TABLE nama_tabel (
			'nama_kolom1' tipe_data(panjang) atribut_lain
			'id' int PRIMARY KEY AUTO_INCREMENT
		)
	c. Insert data
		-> INSERT INTO nama_table VALUES (
			'nama_kolom' = 'data',
		)
	d. Change data Table
		-> ALTER TABLE nama_table CHANGE COLUMN old_name new_name tipe_data(panjang);
	e. Update data Table
		-> UPDATE datafilm SET colom1 = value1, colom2 = value2 WHERE id = id_data; 
	f. ADD COLUMN
		-> ALTER TABLE datafilm ADD COLUMN nama_column TIPE_DATA(Panjang);
3. Paginasi
	a. Batasi query dengan LIMIT $index_awal $jumlah_data_tampil
	b. Jika $_GET['halaman'] ada, maka $index_awal dirubah
		-- Penentuan rumus
		*misalkan perhalaman 5 data ditampilkan
		1. Halaman 1 = index dari 0
		2. Halaman 2 = index dari 5
		3. Halaman 3 = index dari 10
		dst ...

		-- Analisa 1
		1 ... 5 = 0
		2 ... 5 = 5
		3 ... 5 = 10

		operator apa yang cocok untuk ... ?? apakah +, -, *, atau / ??
		
		-- Analisa 2
		1 * 5 = 5  -> harusnya 0            1 * 5 = 5 - 5
		2 * 5 = 10 -> harusnya 5    jadi -> 2 * 5 = 10 - 5
		3 * 5 = 15 -> harusnya 10			3 * 5 = 15 - 5

		-- Analisa 3
		(1 * 5) - 5 = 0
		(2 * 5) - 5 = 10   
		(3 * 5) - 5 = 15

		-- Hasil Akhir
		$index_awal = $halaman * $jumlah_data_tampil - $jumlah_data_tampil
	
	c. Tambah navigasi untuk berpindah halaman
		1. Ada berapa halaman?
		2. Gunakan ceil(count($hasil_fetch_array_dari_query) / $jumlah_data_tampil), ceil adalah pembulatan ke atas
		3. Gunakan for untuk menampilkan navigasi
		4. Kirim data halaman menggunakan Get untuk merubah index index_awal
		5. Kirim user ke halaman yang sama dengan '?halaman=$integer_navigasi
		6. Amankan jika ada user nakal, memberi nilai pada "?halaman=" yang lebih kecil dari 1 misal -100, maka paksa agar tetap 1.		 
4. Fitur tambah data 
	a. Tambahkan tombol
	b. Siapkan form untuk tambah data
	c. Siapkan pemrosesan data gambar
		1. Cek ukuran file
		2. Cek extensi file
		3. Rubah nama file menjadi nama_file_lama-kode_acak_php
		4. Jadikan pemrosesan file ke fungsi lain, agar bisa digunakan di tugas lain.
		5. Jadikan nama file yang baru sebagai output fungsi file
		6. Simpan file gambar di folder yang diinginkan
		7. Kirim nama gambar untuk disimpan di database bersamaan dengan data lain
	d. Perbaiki dan tambah tabel untuk gambar di index.php
	e. Kirim 'message'/'error' ke index.php jika pemrosesan selesai
5. Fitur edit
	a. Bikin halaman editnya
	b. Ambil data dari database sesuai id data yang dipilih
	c. Simpan id dan nama gambar ke hidden input untuk disimpan ulang
	d. Timpa gambar lama dengan gambar baru jika file baru telah diupload
	e. Update data dari database yang memiliki id dari hidden input
	f. kembalikan ke halaman index dan beri pesan berhasil
6. Fitur hapus	
	a. Ubah tombol hapus agar sesuai edit
	b. Kirim data id ke controller hapus
	c. Berikan konfirmasi javascript
	d. Kirim ke halaman index dengan pesan berhasil terhapus



*Additional:
1. Bikin fungsi untuk query data agar bisa buat berulang kali di file lain dan panggil menggunakan require
2. Pisahkan CSS ke file baru agar hmtl rapih